# G2-B: NeMo `diar_msdd_telephonic` v1.0.1 -- not yet implemented

Status per `MODEL_SELECTION_AND_INFERENCE.md`: "Pending balanced pilot;
telephone-domain specialization must be reported." Third priority after
G3-A and G4-A.

This directory is a placeholder to match the interface layout in
`inference/README.md`. No runner, adapter, or environment files exist here
yet -- `run_model.py --system G2-B` will fail with a clear "not implemented"
error rather than silently doing nothing.
